import { render } from "@testing-library/react"
import GiftExpertApp from "../src/GiftExpertApp";

describe('Pruebas en <GiftExpertApp />', () => {
    test('should ', () => {
        render(<GiftExpertApp />);
        // tomar snap shot opcional
        // probar el onAddCategory
        // manipular el formulario
        // que pasa cuando introduz una categoria diferente
        // maximo 20 min
    })
    
})
